This image shows the Upload Script dialog. The dialog includes a button to browse to the script file in the local directory. The dialog also includes fields that display the name of the script file once you browse and select it. It also includes a File Character Set field, which is populated by default with the option Unicode UTF-8.  In addition, the dialog includes Upload and Cancel buttons.